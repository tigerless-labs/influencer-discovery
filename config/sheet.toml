# 逻辑字段 → 表头文字。随代码走。
# 目标表标识与凭据随安装走,住 ~/.config/outreach/.env:
#   OUTREACH_SPREADSHEET_ID
# tab 的 gid 运行时向 API 查,不在这里存 —— 存了会随改名与挪位静默指错表。

tab = "Target List"

[columns]
display_name = "Name"
channel = "Platform"
profile_url = "Link"
contact = "Contact"
contact_kind = "Contact Type"
status = "Status"

# 身份与去重

目标的键是**平台加平台内原生标识符**。显示名、昵称、邮箱都不作键:名字会改,邮箱会换,
平台原生标识符不会。当前 Sheet 以显示名充当事实上的键,这是重复行与状态对不上的根源。

同一个人出现在多个平台是常态,合并只在有**可验证的连接证据**时发生:本人主页上的互链、
平台间公开绑定的账号。不靠名字相似度猜测——猜错的代价是把两个人当成一个人、或对同一个人
重复发信,都比漏掉一个人严重。

求不出稳定键的记录不丢弃,进待裁决池,由人补齐标识符或明确弃用。它们永远不进主表:无键
即无法去重,下一轮抓取会把它们再带进来一次。

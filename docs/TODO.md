# TODO

- [x] 首批设计文档:五阶段流水线、身份与去重、取数层、Sheet 契约。
- [x] 确定写哪张表:**目标表**(实体),不是联系记录表(事件)。零列改动。
- [x] 确定范围:止于拿到联系方式;发信、分层、成交跟踪、点击结算全部划出。
- [x] 去重方案定案:`(人, 平台)` 查 log,不解析链接。实测按 URL 去重会误杀真人。
- [ ] log 的落地格式与首次播种(用目标表现有行的名字与平台)。
- [ ] 种子:目录型的入口 URL、搜索型的查询词表。词表现在不存在——`Topic Area` 是自由文本,
      大多只出现一两次,不能直接当词表用。
- [ ] 一次性回填:把只在联系记录表里出现、不在目标表的那批人按名字补进目标表,状态标已联系。
      这批无主页链接,只能靠名字兜底(只标记不丢弃)。
- [ ] 目标表现存重复:按主页链接有一批重复行。流水线只保证不新增,历史清理是人的决定。
- [ ] 实现顺序:先视频站公开频道页,再赞助位市场目录——这两条已被现有数据证明走得通且不需登录。
- [ ] Sheet 读写模块,先对副本跑通再碰正式表。
- [ ] 补 `docs/testing.md` 与测试映射;需要时再建 `docs/bugs/`、`dev_skills/`、`workflows/`。

## skill(`skills/outreach/`)

- [ ] 本地接上:仓库根的 `skills/` 不被 Claude Code 自动发现,要做软链或 marketplace 配置。
- [ ] 数据层逐个探索并回填 `reference/datalayer/`。已确定边界:Instagram、TikTok
      (均走 ScrapeCreators)、Reddit(`rdt` CLI)、LinkedIn、X。
      **免认证那一档只写了结论,各渠道的具体入口未跑通。**
- [ ] `reference/methodology/` 里除已实测的几份外仍是骨架,正文混着未验证的经验断言
      (页面结构、字段名、路径名)。用之前先核实。

## 渠道分类收窄的余项

计划见 [plans/2026-08-06-channel-taxonomy.md](plans/2026-08-06-channel-taxonomy.md)。

- [ ] 目标表现有 62 行 Website 有多少符合收窄后的「分发站」定义。历史行不清洗,但要知道比例;
      比例太低说明这一档的历史产出大部分是产品站。
- [ ] `media.md` 的域名清单实现后移进 `config/` 的共享域名清单,那份 md 只留判据。
- [ ] **DEV.to 的卖买比例没测** —— 这一档最关键的缺口。已抓到的标题与 tag 分布够判,
      不需要新请求。测出来才知道大 blog 平台排第二这个优先级站不站得住。
- [ ] DEV.to 剔掉 GitHub 那一跳后,只走 `website_url` 的最终邮箱命中率。现有可触达数字
      含 GitHub,不能直接用。
- [ ] Hashnode 的赞助通道有没有**公开可翻的创作者目录**。有的话它比标签页强 ——
      列在那里的人本身就是卖方,省掉卖买判别那一步。
- [ ] freeCodeCamp News 的 559 个作者里有多少还活着(sitemap 收的是历史全量),
      以及自有域名 12/12 换更大样本会掉多少。
- [ ] HackerNoon 与 Bear Blog 都卡在**资料页/发现页对诚实 UA 返回质询页**。不绕。
      若将来接入带登录态的取数层,这两个可以重新评估。
- [ ] **WordPress.com 只有时间序**,样本里混着大量垃圾账号。找不找得到带互动或时间窗的排序,
      决定这个入口能不能真用 —— DEV.to 上这一条是决定性的。
- [ ] WordPress.com → Gravatar 那两成邮箱只在一个标签的三十个名字上测过,换垂类会不会掉。
- [ ] Micro.blog 的翻页与话题取法未探,只取过首屏 50 条。
- [ ] **freeCodeCamp News 与 HackerNoon 归哪一档未定** —— 有编辑审,不满足
      `2-blog-platform/` 的「无门槛」定义,但作者自带受众,也不是 `media.md` 那种多作者刊物。
- [ ] `cost-ranking.md` 只有一个轴(抓取 `credit/人`),缺**合作成本/人**那一轴。
      数据只能从实际谈过的人身上来,属发信侧产出。在有数之前用代理信号:
      有没有 rate card / 招商页。
- [ ] 媒体的作者页当发现源值不值得跑:作者页是高纯度人名列表,但要三跳且中途断掉过半,
      而产出的人还得再去别的平台找他的主场。**未承诺**,现在按不做处理。
- [ ] 公司站挖个人的做法(`/team` 三跳、名单页分流、按站分流的命中率)随目标收窄一起退役,
      原文在 `website.md` 的 git 历史里。若将来要做同行/合作方名单,从那里取回。

## 将来的选项

- **与 auto-gtm 合并**(已定方向)。它的取数层已解决认证:复用浏览器登录态、单一入口、分档降级、
  只读白名单。合并后本项目发现阶段改为经它取数,不自建认证。现在不引依赖——它覆盖 X / Reddit,
  与本项目已验证的两条路不重叠。设计已按「取数方式可替换」写好,换的时候其余四阶段不动。
- **认证墙后的平台**(X / LinkedIn / Reddit)。现有数据里合计不到一成,且其中多数链接根本不指向
  那些平台。等合并后随取数层一起获得,不单独投入。

## 待确认

- Sheet 共享设置是「任何人有链接即可编辑」,任何拿到链接的人都能改。表属另一位同事,
  需与其确认是否收紧。
- 目标表的平台列现在混着两种含义(人所在平台 / 打算用的联系渠道)。新行按前者写,历史行是否
  清洗待定。

## 参考件的处置

`build-partnership-tracker`(外部 skill)管的是谈成之后的点击跟踪与结算,从「已确认合作」起步,
装不下候选到联系方式这一段,不引入。它的两个缺陷若要复用其代码需先修:归档内夹带真实联系人
邮箱;硬编码 origin 只被替换了一半,会静默指向他人账号下的域名。
